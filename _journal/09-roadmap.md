# RenForge — роадмап архитектуры (универсальность извлечения/доставки)

> Цель: приблизить (не достичь — это невозможно) 100% извлечения и доставки текста.
> План строится вокруг двух идей и набора инвариантов. Статусы отмечаем по ходу.
> Родственные разделы: 02 (экстрактор), 08 (краевые случаи движков), 06 (решения).

## Инварианты (жертвуем — значит это уже ДРУГОЙ продукт)
- Оффлайн, без Ren'Py SDK и без установки Python у пользователя.
- Редактируемый человеческий перевод, распространяемый как мод.
- Crash-safe доставка; язык игры не переключаем (не травим `_preferences.language`).
- Универсальность Ren'Py 6.10 → 8.5. Регресс-дисциплина (bench). 6 локалей синхронно.
- → Живой MT/OCR-оверлей ВНЕ роадмапа (нарушает инварианты; отдельный продукт).

## Принципы
1. **Измеряй, прежде чем строить вслепую.** Сначала делаем покрытие измеримым на конкретной
   игре, потом бьём туда, куда показывают данные.
2. **Аддитивно и обратимо.** Новые каналы — за флагом, с try/except и фоллбэком; работающее не ломаем.

## Ключевой инсайт (почему 100% недостижимо)
Извлечение статично (литералы из AST), доставка — рантайм-матчинг по ТОЧНОМУ тексту.
Мост — исходный текст. Ren'Py-интерполяция `[var]` берётся отлично (say-фильтр видит текст ДО
подстановки → переводим шаблон). Слепая зона — сборка на стороне Python (`+`/`.format()`/`%`):
строка становится целой ДО любого хука. Универсальное «горлышко» есть только на слое рендера
(`Text`-displayable), но там текст уже ФИНАЛЬНЫЙ (значения подставлены) → «увидеть можно всё,
перевести — нет» (ключи неограниченны). Отсюда фазы ниже.

---

## Фазы

### Фаза 0 — Измеримость + расчистка инфры (ФУНДАМЕНТ)
- **0.0 · Починить `bench.py`** — пути (EXTRACTOR указывал на несуществующий `extractor/scr/main.py`)
  + пул игр под реально лежащие. Пререквизит для любых правок извлечения (2.2/3.1). *V:сред R:низк E:низк.*
- **0.1 · Лог непокрытого на семантических горлышках** — инструментируем say-фильтр /
  `translate_string` / `ui.text` / `input`: строки, что прошли, но их нет в словарях → файл +
  отчёт в UI (opt-in «диагностическая сборка»). Классификация против БД: «видно в игре, но нет
  в базе» = кандидаты. *V:ВЫСОКАЯ R:низк E:сред.* Ключевой камень.
- **0.2 · Рендер-хук диагностика** — хук `Text` → лог ВСЕГО финального нематченного (видит
  динамику/кастомный рендер). Отдельный тяжёлый режим (шум, hot-path). Фундамент под 3.1. *V:выс R:сред E:ср-выс.*

### Фаза 1 — Дешёвые победы (аддитивные каналы, по данным 0.x)
- **1.1 · Канал 4** — хук `translate_string`/`store.__` для голого screen-текста (пост-фреймворковые движки). *V:сред R:низк E:низк-ср.*
- **1.2 · Предупреждение о мультиязычной коллизии** — `target ∈ available_languages` → тост. *V:сред R:низк E:низк.*
- **1.3 · Репорт пропущенных файлов экстрактора в UI** — частично есть (stderr после фикса #1), довести. *V:сред R:низк E:низк.*

### Фаза 2 — Легаси-охват (ГЕЙТ: легаси < ~2013 в приоритете)
- **2.1 · Дешёвый легаси-UI слой** — override `_()`/`__()` в сторе (ловит старые игры на `_()`). *V:сред→низк R:низк-ср E:сред.*
- **2.2 · SL1 source-preservation** — сохранять `PyCode.source`, парсить литералы экранов (6.12–6.17). Правка анпиклера → ОБЯЗАТЕЛЬНЫЙ bench (зависит от 0.0). *V:сред R:сред E:ср-выс.*

### Фаза 3 — Фронтир динамики (крупный лифт; ГЕЙТ: данные 0.1/0.2 оправдывают)
- **3.1 · Pattern / format-aware доставка** — извлекаем шаблоны (`.format()`/`%`/`_()`), на рендере
  (зависит от 0.2) реверс-матчим фиксированные части, вытаскиваем переменные, применяем
  переведённый шаблон. Единственный принципиальный путь к динамике. *V:выс/узк R:ВЫСОКИЙ E:высокое.*

---

## Сводка и статус

| # | Пункт | V | R | E | Гейт | Статус |
|---|---|---|---|---|---|---|
| 0.0 | Починить bench.py | сред | низк | низк | — | ✅ готово |
| 0.1 | Лог непокрытого (семантика) | выс | низк | сред | — | ✅ готово |
| 0.2 | Рендер-хук диагностика | выс | сред | ср-выс | — | 🔵 план |
| 1.1 | Канал 4 (= K6) | сред | низк | низк-ср | по 0.x | ✅ покрыто K6 |
| 1.2 | Предупреждение мультиязычия | сред | низк | низк | — | ✅ готово |
| 1.3 | Репорт пропущенных файлов | сред | низк | низк | — | 🟡 в исходниках, ждёт пересборки сайдкара |
| 2.1 | Легаси `_()`-слой | сред→низк | низк-ср | сред | легаси-приоритет | 🔵 план |
| 2.2 | SL1 source-preserve | сред | сред | ср-выс | 0.0 + легаси | 🔵 план |
| 3.1 | Pattern-доставка | выс/узк | выс | выс | данные 0.x | 🔵 план |

**Старт:** 0.0 + 0.1 (дёшево, разблокирует измеримую приоритизацию всего остального).

**Сделано (v1.3.0-dev):**
- **0.0** — `bench.py` починен (путь EXTRACTOR → `renforge/src-tauri/tools/extractor/main.py`,
  авто-дискавери игр из `GAMES_ROOT` вместо устаревшего хардкода). Проверено: list/run/snapshot/compare
  работают, 5 игр, baseline пере-снят, compare = ALL MATCH.
- **0.1** — opt-in «диагностическая сборка»: рантайм-логгер `_renforge_log_uncovered(chan,s)`
  вплетается в доставку (реальный при диагностике, no-op иначе) и зовётся на промахах каналов
  say (K1) / translate_string (K6) / input (K3) / ui.text (K5). Лог `renforge_uncovered.log`
  в корне проекта; команды `read_uncovered` (сверка с БД → `in_db`/`translated`) и `clear_uncovered`;
  модалка «Непокрытый текст» (фильтр «кандидаты» = видно в игре, но нет в базе, + добавить в ручные).
- **1.2** — предупреждение о мультиязычной коллизии: computed `targetLangCollision` в сторе (target ∈
  available_languages, без учёта регистра, исключая 'original') + постоянный баннер на дашборде под
  инфо-баром (`.lang-collision-bar`, янтарный). Локаль `lang_collision_warn` ×6. Выбрал постоянный
  баннер вместо тоста из плана — заметнее и не назойливо. Доставку не меняли (только предупреждаем).

**Анализ 1.1 и 1.3 (v1.3.0-dev):**
- **1.1 (Канал 4)** — по сути УЖЕ реализован как K6 (`RENFORGE_EARLY_HOOK`, приехал в 1.2): перехват
  `store._`, `store.__`, `renpy.translation.translate_string` с языконезависимой подменой по ui_map.
  Отдельный канал строить не нужно → помечаю «покрыто K6». Единственный остаток (низкий приоритет):
  K6 сохраняет как оригинал только `_` и вешает общую синглульную обёртку и на `__`; если `__` имеет
  иную семантику/арность (в Ren'Py это отдельная функция перевода), обёртка неточна, а фоллбэк
  `_renforge_ts_orig(s, *args)` не обёрнут в try. Мелкая закалка на будущее (сохранять/звать реальный
  `__`, обернуть фоллбэк), не новый пункт роадмапа.
- **1.3 (репорт пропущенных файлов)** — нужность СРЕДНЯЯ, риск НИЗКИЙ. Данные частично есть: `main.py`
  (после фикса #1) пишет пропуски в stderr (`[WARN] Пропущен файл (сбой разбора AST): <path> -> <err>`
  + итоговый счётчик), НО `extract_and_ingest_project` читает stderr ТОЛЬКО при ошибке — на успехе
  пропуски теряются. Плюс сейчас логируются только сбои `explore_ast`, а пустой `load_ast` (tree
  falsy) молча пропускается и не считается. Путь (Option B, рекомендую): экстрактор кладёт
  `skipped_files` в выходной JSON → поле в `ExtractedData` → возврат на фронт → тост/панель «N файлов
  не разобрано». Требует пересборки сайдкара + bench (0.0 готов). Аддитивно, строки не меняются.
  Option A (без пересборки) — парсить stderr на успехе — быстрее, но хрупко (free-text).

**1.3 — реализован в исходниках, ждёт пересборки сайдкара:** `skipped_files` сквозь main.py →
models.rs → lib.rs (`ExtractResult{total,skipped_files}`) → actions.js (warn-тост) → локали ×6.
`cargo check`+`vite build` чисты. Требует пересборки `rpyc_extractor.exe` + `bench.py`
run/compare (регресс-дисциплина) перед коммитом-тестом владельца.

**Следующий кандидат:** 0.2
(рендер-хук диагностика — тяжелее, но двигает к динамике 3.1). 1.1 закрыт как покрытый K6.
